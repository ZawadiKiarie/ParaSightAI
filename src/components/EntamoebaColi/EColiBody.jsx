/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/models/EColiBody-v1.glb -o src/components/EntamoebaColi/EColiBody.jsx -k -r public 
*/

import React, { useEffect } from "react";
import { useGLTF, useAnimations } from "@react-three/drei";
import { DirtyCytoplasmMaterial } from "./DirtyCytoplasmShader";

export function EColiModel(props) {
  const group = React.useRef();
  const { nodes, _materials, animations } = useGLTF("/models/EColiBody-v1.glb");
  const { actions } = useAnimations(animations, group);
  // console.log(actions);

  useEffect(() => {
    const actionNames = ["MeshAction", "MeshAction.001"];

    actionNames.forEach((name) => {
      const action = actions?.[name];
      if (action) {
        action.reset().fadeIn(0.5).play();
        console.log(`Animation found: ${name}`);
      } else {
        console.warn(`Animation not found: ${name}`);
      }
    });

    return () => {
      actionNames.forEach((name) => actions?.[name]?.fadeOut(0.5));
    };
  }, [actions]);
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <mesh
          name="outermembrane"
          geometry={nodes.Mball002.geometry}
          // material={nodes.Mball002.material}
          morphTargetDictionary={nodes.Mball002.morphTargetDictionary}
          morphTargetInfluences={nodes.Mball002.morphTargetInfluences}
          position={[0, 1, 0]}
        >
          <meshPhysicalMaterial
            morphTargets={true}
            color={"#c2cf75"}
            transparent={true}
            opacity={0.8}
            transmission={0.1}
            thickness={1.0}
            roughness={0.2}
            metalness={0}
            ior={1.33}
            depthWrite={false}
          />
        </mesh>
        <mesh
          name="innermembrane"
          geometry={nodes.Mball001.geometry}
          // material={nodes.Mball001.material}
          morphTargetDictionary={nodes.Mball001.morphTargetDictionary}
          morphTargetInfluences={nodes.Mball001.morphTargetInfluences}
          position={[0, 1, 0]}
        >
          <DirtyCytoplasmMaterial
            opacity={0.88}
            noiseScale={13.0}
            flowSpeed={0.96}
            brightness={0.2}
            color="#c2cf75"
            fresnelStrength={0.06}
            morphTargets={true}
          />
        </mesh>
      </group>
    </group>
  );
}

useGLTF.preload("/models/EColiBody-v1.glb");
